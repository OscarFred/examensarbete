import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // lists: {}
  },
  mutations: {
    // newList (state, title, list) {
    //   state.lists.push(list)
    // }
  },
  actions: {},
  modules: {}
});
